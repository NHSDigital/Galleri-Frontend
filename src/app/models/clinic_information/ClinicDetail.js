// Clinic Summary model
export default class ClinicDetail {
  constructor(
    clinicId,
    clinicName,
    address1,
    address2,
    postcode,
    weeklyCapacity
  ) {
    this.clinicId = this.clinicId;
    this.clinicName = this.clinicName;
    this.address1 = this.address1;
    this.address2 = this.address2;
    this.postcode = this.postcode;
    this.weeklyCapacity = this.weeklyCapacity
  }
}
